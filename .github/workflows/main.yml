name: Ubuntu SSH Hub

on: 
  workflow_dispatch:

jobs:
  ssh-access:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install OpenSSH Server
      run: |
        sudo apt update
        sudo apt install -y openssh-server
        sudo service ssh start
        echo "root:Root123" | sudo chpasswd  # Set password for root

    - name: Allow root login
      run: |
        sudo sed -i 's/#PermitRootLogin prohibit-password/PermitRootLogin yes/' /etc/ssh/sshd_config
        sudo service ssh restart  # Restart SSH service for changes to take effect

    - name: Install Cloudflared
      run: |
        wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
        sudo mv cloudflared-linux-amd64 /usr/local/bin/cloudflared
        sudo chmod +x /usr/local/bin/cloudflared

    - name: Create config.yml
      run: |
        mkdir -p ~/.cloudflared
        echo "tunnel: 7d92c3d4-daea-488e-bd1f-a78c6c673adf" > ~/.cloudflared/config.yml
        echo "credentials-file: /root/.cloudflared/7d92c3d4-daea-488e-bd1f-a78c6c673adf.json" >> ~/.cloudflared/config.yml
        echo "" >> ~/.cloudflared/config.yml
        echo "ingress:" >> ~/.cloudflared/config.yml
        echo "  - hostname: demo.andik252.biz.id  # Subdomain yang ditetapkan di Cloudflare" >> ~/.cloudflared/config.yml
        echo "    service: ssh://localhost:22  # Port SSH" >> ~/.cloudflared/config.yml
        echo "  - service: http_status:404" >> ~/.cloudflared/config.yml

    - name: Authenticate Cloudflared
      run: |
        echo "${{ secrets.CLOUDFLARE_TOKEN }}" | cloudflared tunnel login

    - name: Run Cloudflare Tunnel
      run: |
        cloudflared tunnel --config ~/.cloudflared/config.yml run

    - name: Keep alive
      run: |
        while true; do sleep 60; done
